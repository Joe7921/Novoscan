/**
 * 已弃用组件（已清理删除）
 *
 * 以下组件已在模块化重构中确认无引用并删除：
 *   - ads/          广告组件（空壳，返回 null）
 *   - auth/         认证组件（空壳，去登录化后移除）
 *   - partner/      合伙人表单（SiteFooter 引用已清理）
 *   - skill-check/  技能检查（CTO 体检，组件无外部引用）
 *
 * 此目录仅作为分类标记保留，无实际导出。
 */

export {};
